{"auditor": "labelaudit-r2-01", "cause": "label", "confidence": "high", "evidence": "The Dafny loop `while m > k { m := m - k; k := k + 1; }` terminates once the triangular number k(k+1)/2 passes n, giving Theta(sqrt(n)) iterations rather than n; the Python has the byte-identical while-loop with the same k increment, so it is Theta(sqrt(n)) too, meaning the label is wrong about the Python it was measured on.", "label": "O(n)", "sid": "5_100", "true_class": "other", "verdict": "mismatch", "what_to_look_for": "The label assumes the loop runs on the order of n times, but k grows by 1 every pass while m falls by k, so it stops once k(k+1)/2 exceeds n -- Theta(sqrt(n)) iterations, not n. Instrument the `while m > k` loop with a counter for a large n (e.g. n=10**6) and check the count lands near sqrt(2n) (~1414) rather than near n; if so the tight class is O(sqrt(n)), which is outside this dataset's vocabulary."}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "translation", "confidence": "high", "evidence": "The translated binary search `while lo < hi` halves the [0, num+2) range each iteration to recover the integer square root, giving O(log(num)) iterations, while the Python it replaces computes the same value with a single O(1) call to math.sqrt, so O(1) held for the Python but not for this Dafny loop.", "label": "O(1)", "sid": "5_25", "true_class": "O(logn)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "SortInts(ignored_lines) is the only nontrivial cost, an O(n log n) merge sort per the cost table, followed only by O(1) SumSeq-derived arithmetic and index reads; the Python's sorted(a) after an O(n) input loop is the same O(n log n) shape.", "label": "O(nlogn)", "sid": "61_161", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "MaxSeq(l) and SumSeq(l) are the only prelude calls, each O(n) by the cost table, with no sort and only O(1) arithmetic and division afterward; the Python's max(l) and sum(l) over the same list are the same linear shape.", "label": "O(n)", "sid": "61_295", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "SortInts(a_list) runs unconditionally before the loop at O(n log n), and the loop's slice-and-compare `a_list[x..] + a_list[..x]` costs only O(n) and fires at most once because doneFlag ends the loop; the Python calls sorted(a) once when the mismatch is found, so both sides are O(n log n).", "label": "O(nlogn)", "sid": "71_217", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "The single while loop over i from 0 to n does O(1) work per iteration (one index compare and two counter updates) with no sort or seq copy, matching the Python's single for-loop over range(n) with O(1) work per index.", "label": "O(n)", "sid": "71_257", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "translation", "confidence": "high", "evidence": "dArr is declared as seq<int> of length b and is written with `dArr := dArr[q3 := ...]` or `dArr[q4 := ...]` inside the while loop over c_list, and each such seq index-update copies all b elements, so a loop of length n performing up to n such updates costs O(n*b) = O(n*m); Python's equivalent `d[q4] += 1` mutates a list element in place in O(1), matching the label O(n) that Dafny no longer achieves.", "label": "O(n)", "sid": "85_141", "true_class": "O(n*m)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "translation", "confidence": "high", "evidence": "answer is a seq<int> sized to the taxi count and is updated with `answer := answer[idx := answer[idx] + 1]` inside the while loop over passengers, so each of up to n passenger iterations pays an O(m) copy of answer, giving an O(n*m) term that dominates the O(n log n) sorts and the O(log m) binary searches; Python's `answer[index] += 1` is an O(1) in-place list write, matching the O(nlogn) label that only held for the Python.", "label": "O(nlogn)", "sid": "85_71", "true_class": "O(n*m)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "medium", "evidence": "ReverseString recursively takes s[1..] and appends s[0] at every level -- the same shape as the 888_6 ReverseSeq trap -- so reversing a string of length L costs O(L^2), and in the worst case (one long string among the 2n inputs) this dominates the O(1)-per-character scan loops, matching the O(n**2) label under total-input-size scaling. The Python instead reverses with x[::-1], a single O(L) slice, so it is linear where the Dafny is quadratic.", "label": "O(n**2)", "sid": "88_200", "translation_defect": true, "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "label", "confidence": "medium", "evidence": "This solves the identical problem with the identical shape as sibling solution 88_200 -- an outer loop over n test cases, each doing a constant number of O(string-length) reversal and scan operations via ReverseString, bRev-scanning and aRev-scanning -- and 88_200's equivalent pattern is labelled O(n**2) for exactly this shape, so a plain O(n) label here omits the per-test string-length dimension that both Python's `b[::-1].index(\"1\")`/`a[k::].index(\"1\")` and the Dafny while loops genuinely scan.", "label": "O(n)", "sid": "88_21", "true_class": "O(n**2)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "label", "confidence": "high", "evidence": "Each of the n loop iterations reads only the five fixed positions k[0] through k[4] of the current row and never loops over the row's own length, so row width m never enters the cost in either the Dafny or the Python, which only ever indexes k[0], k[1], k[2], k[3] and k[-1].", "label": "O(n*m)", "sid": "89_463", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "The while loop runs n times, each iteration reading five already-parsed integers from lists[i] and doing O(1) arithmetic and comparisons, plus a Join over n O(1)-length strings; the Python's for loop over n lines does the same constant-size split and arithmetic per line.", "label": "O(n)", "sid": "89_909", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "label", "confidence": "low", "evidence": "`Sort(b_list, ...)` costs O(m log m), but inside the while loop over i, `l := l[i := kSorted[ptr]]` copies all n elements of l on every triggered index and can trigger up to min(n,m) times, giving an O(n*m)-order term that dominates the labelled O(nlogn+mlogm); Python's own `k.pop(0)` inside the same conditional shifts up to m elements per pop and likewise produces an O(n*m)-order worst case the original label did not account for, so both sides exceed the stated bound for the same underlying reason.", "label": "O(nlogn+mlogm)", "sid": "101_10", "true_class": "O(n*m)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "label", "confidence": "medium", "evidence": "Only `b_list[0]` is ever read and no loop indexes further into b_list, so m never contributes to the cost though it is an argument; the executing branch is only reached when `CountZeros(a_list) <= 1`, so the seq update `a := a[i := bv]` fires at most once and does not add a term, leaving both loops at O(n); Python shows the identical single use of b[0] and the same count(0) <= 1 guard, so the +m in the label names a dimension neither side scans.", "label": "O(n+m)", "sid": "101_43", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "label", "confidence": "high", "evidence": "IntSqrtFloor is an O(log n) binary search, but the following loop `while i < x && p == 0 { ...; i := i + 1; }` runs up to x = floor(sqrt(n)) times, making the whole method Theta(sqrt(n)) rather than Theta(n); the Python's `for i in range(1, x)` has the identical bound x = floor(sqrt(n)).", "label": "O(n)", "sid": "167_177", "true_class": "other", "verdict": "mismatch", "what_to_look_for": "The label assumes the search loop scans on the order of n candidates, but it is bounded by x = floor(sqrt(n)), not n, via `while i < x && p == 0`. Instrument the loop counter i for a large n (e.g. n=10**8) and check it tops out near sqrt(n) (~10000) rather than near n; if so the tight class is O(sqrt(n)), and it is the label (measured on the same Python range(1,x) loop) that is wrong, not the translation."}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "The body is a single if/else with two O(1) arithmetic operations and no loop or recursive call, and IntToString is applied to one O(1)-digit value; the Python's calculate() is likewise a single conditional returning a constant-size formatted string.", "label": "O(1)", "sid": "167_191", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "The while loop runs n times over already-split pairs, doing O(1) mod arithmetic per iteration and an O(1) deferred append to parts (no s[i] read in that loop); the Python's for loop over n lines performs the identical per-line O(1) arithmetic.", "label": "O(n)", "sid": "171_0", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "label", "confidence": "high", "evidence": "Identical structure to sibling solution 171_0 -- the single while loop reads only the two fixed positions l[0] and l[1] of each row and never scans row width, so m never enters the cost in either the Dafny or Python's fixed two-field unpacking `a = int(l[0]); b = int(l[1])`.", "label": "O(n*m)", "sid": "171_82", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "SortInts(a_list) is the only nontrivial operation, an O(n log n) merge sort, followed by a single O(1) index into the sorted result; the Python's a=sorted(a) after an O(n) int-cast loop is likewise dominated by the O(n log n) sort.", "label": "O(nlogn)", "sid": "187_193", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-batch-01-sonnet", "cause": "translation", "confidence": "high", "evidence": "arr is a seq<int>, and inside the nested while loops over i and j (O(n^2) iterations), every triggered swap performs `arr := arr[i := arr[j]]` and `arr := arr[j := tmp]`, each an O(n) full-sequence copy, so the real cost is n^2 trigger iterations times an O(n) copy, worse than quadratic and only approximable by the largest available vocabulary entry O(n**2); Python's `t=list[i]; list[i]=list[j]; list[j]=t` performs the same swap with O(1) in-place writes, which is why O(n**2) is correct for the Python but not for this seq-based translation.", "label": "O(n**2)", "sid": "187_762", "true_class": "O(n**2)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "The body is one expression using |s|, which never forces a copy per the cost model, plus O(1) constant-count arithmetic and no loop; the Python computes len(q) (O(1) in CPython) and the same fixed arithmetic expression.", "label": "O(1)", "sid": "208_290", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-batch-02", "cause": "translation", "confidence": "high", "evidence": "The statement a := a[i + 1 := cand] inside the while i<N-1 loop is a functional seq update copying all N entries each iteration, giving O(n**2) total; Python's a[i+1] = ... is an O(1) list index assignment, so O(nlogn) (dominated by the initial sort) is right for the Python and wrong for this Dafny.", "label": "O(nlogn)", "sid": "209_103", "true_class": "O(n**2)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "Each pass of `while |a| > 0` removes exactly one element via the O(|a|) slice-concat `a[..mxIdx] + a[mxIdx+1..]` and rebuilds newA with another O(|a|) scan, so the n outer passes over a shrinking sequence sum to O(n**2); the Python's rng.remove(mx) plus the following for-loop over rng are the same O(len(rng)) operations per pass, giving the identical O(n**2).", "label": "O(n**2)", "sid": "209_29", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "The single while loop over a_list does four O(1) comparisons and counter increments per index with no sort or nesting, and the Python's for loop over j performs the same four O(1) comparisons per element, so both are linear in n.", "label": "O(n)", "sid": "223_180", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-02", "cause": "translation", "confidence": "high", "evidence": "s := s[j := s[j] + s[i]] inside the while i<j loop is a seq update copying all of s on up to n iterations, giving O(n**2); Python's s[j] += s[i] is an O(1) list index update, so O(nlogn) (SortInts plus a linear scan) is right for the Python and wrong for this Dafny.", "label": "O(nlogn)", "sid": "223_3085", "true_class": "O(n**2)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "SortInts(a_list) costs O(n log n), and the subsequent scan plus the recursive SumRange calls are each O(n), so O(n log n) dominates; the Python's a.sort() plus sum(a[i:])/sum(a[:i]) slicing perform the same O(n log n) plus O(n) work.", "label": "O(nlogn)", "sid": "241_101", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "There is no sort; the allPos/allNeg scan, MinSeq, MaxSeq, and the fallback absolute-value loop are each a single O(n) pass over a_list with no nesting, matching the Python's all()/sum()/min()/max() calls or the single for-loop fallback, all O(n).", "label": "O(n)", "sid": "241_47", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-02", "cause": "label", "confidence": "high", "evidence": "Repeat('4', z + w * 2) + Repeat('7', x - w) builds output whose length is proportional to the magnitude of n (x = n/7), costing O(n) per the Repeat cost rule; sibling row 271_58 for the same problem uses the identical Repeat pattern and is labeled O(n), and Python's '4'*(...) string multiplication is equally O(n), so O(1) is wrong for both.", "label": "O(1)", "sid": "271_51", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "Repeat is charged O(length of its count argument) by the cost table, and numFour and numSeven are both proportional to the input value n, so the two Repeat calls together cost O(n); the Python's two for-loops printing numFour and numSeven characters do the identical O(n) output work.", "label": "O(n)", "sid": "271_58", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-02", "cause": "label", "confidence": "high", "evidence": "abc_list rows are pinned to length 3 by requires forall r :: |r| == 3, so MaxSeq/MinSeq/CountEq/IndexOfEq and the seq updates on ls1 all cost O(1) regardless of any 'm'; the loop over n rows plus Join is O(n), and Python's max/count/index calls on a 3-element ls are equally O(n) overall, so O(n*m) names a row-width dimension that never varies.", "label": "O(n*m)", "sid": "276_1206", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "Each of the n loop iterations works on a row constrained to exactly 3 elements (`|r| == 3` in the requires clause), so MinSeq/MaxSeq on that row are O(1) rather than O(row length), and the parts concatenation has no in-loop read; the Python similarly unpacks x,y,z from a fixed 3-tuple per test case, so both are linear in the number of test cases.", "label": "O(n)", "sid": "276_610", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-02", "cause": "translation", "confidence": "medium", "evidence": "no := no[j := iv] (and the modulo-branch update) sit inside the doubly-nested oi/j loops, so each seq update copies all n entries across an O(n**2) iteration count, giving O(n**3) per convergence pass; Python's no[j] = ... is an O(1) list index assignment giving O(n**2) per pass as the O(n**2) label assumes, so the label fits the Python and this Dafny is a cubic-in-n translation regression.", "label": "O(n**2)", "sid": "281_12", "true_class": "other", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "high", "evidence": "The translator replaced the Python's `for x in range(p, q): ... break` with a direct formula threshold = max(p, 2r), so the Dafny's only costs are MaxSeq(c_list), MinSeq(d_list) and MinSeq(c_list), each O(n) or O(m), giving O(n+m) with no loop at all.", "label": "O(n+m)", "sid": "305_284", "true_class": "O(n+m)", "verdict": "ok"}
{"auditor": "labelaudit-r2-01", "cause": "", "confidence": "medium", "evidence": "SortInts is called on both lists for O(n log n + m log m), and the trailing `while i >= 2*am && amax <= i && i > 0 { i := i - 1 }` is bounded by the numeric gap between list values rather than by n or m, mirroring the Python's byte-identical while-loop; per the value-bounded-loop rule this is treated as O(1) in the input size absent evidence the value range scales with n or m.", "label": "O(nlogn+mlogm)", "sid": "305_76", "true_class": "O(nlogn+mlogm)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "The first while loop grows p by 2 until |p|>=|s| (O(n) iterations), and the second shrinks p by 2 while calling ReplaceAll(cur,p,\"***\") each time, which costs O(|cur|) per the table, giving O(n) iterations times O(n) work each; the Python's s.replace(p,'***') is equally O(len(s)) per call inside the same O(n)-iteration loop, so both are quadratic and match the O(n**2) label.", "label": "O(n**2)", "sid": "307_116", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "The outer while loop runs a fixed 100 times regardless of input, and inside it ReplaceAll walks |cur| once per call via its own O(|s|) loop building parts then Join, so total cost is 100*O(n)=O(n); the Python for loop over range(100,0,-1) likewise calls the O(len(s)) str.replace exactly 100 times, matching the O(n) label.", "label": "O(n)", "sid": "307_14", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-02", "cause": "label", "confidence": "high", "evidence": "curr_div increments by 1 on each failed trial division with no sqrt(n) cutoff, so a large prime factor in a forces curr_div up toward the magnitude of a itself before n % curr_div == 0 fires; sibling row 310_51 runs the identical increment strategy for the same problem 797A, explicitly bounded to 2*i<=n, and is labeled O(n) -- the same worst case applies here to both this Dafny and the identical Python increment logic, so O(logn) understates it.", "label": "O(logn)", "sid": "310_121", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "The while loop is bounded by the VALUE n (i<=n/2, h halved by discovered factors) rather than by a sequence length, and per iteration does O(1) int ops (mod, div, an IntToString on a bounded factor), so treating n as the value gives O(n) total; the Python loop has the identical i<=n/2 bound and the same O(1) body, so both match O(n).", "label": "O(n)", "sid": "310_51", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "label", "confidence": "high", "evidence": "Resolved by review with the problem statement: 1 <= a_i <= 100, so the inner loop bounded by x/2 runs at most fifty times regardless of n. The row is O(n) in both Dafny and Python, not O(n**2).", "label": "O(n**2)", "sid": "342_149", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label assumes the inner loop grows with n. Read the Input section of the problem statement: it caps a_i at 100 while n reaches 5*10^4, so the inner bound is a constant. If a later revision of the problem lets a_i scale with n, the O(n**2) label stands; as written it does not. Sibling 342_86 carries O(n) for the same shape and is the correct one."}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "Resolved by review with the problem statement: 1 <= a_i <= 100 caps every element at 100 regardless of n (n <= 5*10^4), so the inner loop bounded by sqrt(x) runs at most ten times. Outer loop over a_list gives O(n), matching the label.", "label": "O(n)", "sid": "342_86", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-03", "cause": "translation", "confidence": "high", "evidence": "ContainsInt(source, s) linearly scans the seq `source` (length up to idx) on every loop iteration to emulate Python's `s in source`, so total cost is O(n**2); Python uses set() with O(1) average membership and add(), so the Python is genuinely O(n) as labelled and the seq-based translation is the defect.", "label": "O(n)", "sid": "348_21", "true_class": "O(n**2)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "ContainsInt(start,a) and ContainsInt(end_,b) each scan up to |start|=idx elements before the idx-th append, and because start/end_ are both appended to and read via indexing within the same outer while loop, the table's 's:=s+[x] with an s[i] read in the same loop' rule applies, making each append O(|s|) as well; summed over n iterations this is O(n**2), matching the Python's list.append plus 'x in list' linear scan, which is also O(n) per iteration.", "label": "O(n**2)", "sid": "348_33", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "Solve has no loops or recursion; it computes m and n from two multiplications and a subtraction, each O(1) per the int-arithmetic charge, and formats them with IntToString, so the whole method is O(1), matching the Python's three arithmetic lines with no loop.", "label": "O(1)", "sid": "354_35", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "Each outer-loop step decreases nn by exactly power=Log2Floor(nn), and Log2Floor itself costs O(power) since its own doubling loop runs power times, so work done equals nn's decrease at every step; telescoping over the whole run gives total Dafny cost O(n) exactly. Log2Floor reimplements what Python's math.log2 does as a single O(1) library call, so Python's true cost is O(#iterations)=O(n/log n), strictly less than Dafny's O(n) -- the manual log-loop is real per-call overhead the translation introduced, though both round to the O(n) label since the vocabulary has no O(n/log n) bucket.", "label": "O(n)", "sid": "354_95", "translation_defect": true, "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "label", "confidence": "medium", "evidence": "Both loops touch only pairs[idx][0] and pairs[idx][1] via ParseInt -- no dimension beyond n=|pairs| is scanned -- and map<int,int> lookups/inserts are the only other cost, which the table marks UNMEASURED rather than tied to any m. Sibling 378_91 solves the same problem (432_B Football Kit) with an isomorphic ParseInt+map<int,int> two-loop structure and carries label O(n), so this row's extra 'm' factor is not visible anywhere in the Dafny.", "label": "O(n*m)", "sid": "378_20", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label claims a second dimension m (row width, or the digit-length of the team-id strings parsed by ParseInt) drives the cost, but both loops only ever touch pairs[idx][0] and pairs[idx][1] -- no width or length beyond n=|pairs| is scanned -- and map<int,int> lookups/inserts (the only other cost) are UNMEASURED in this cost model. Check whether team/kit indices in problem 432_B are bounded by n (then any string round-trip via ParseInt is O(log n), not an independent m); also diff this Dafny against sibling 378_91, which has an isomorphic ParseInt+map<int,int> two-loop structure for the same problem and carries label O(n) -- if the structures really are equivalent, this row's extra m factor is a stale label rather than a real cost."}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "Both loops touch only pairs[idx][0] and pairs[idx][1] via ParseInt, and the Counter-equivalent map<int,int> tracks at most n keys with unmeasured but non-scanning inserts/lookups; the Python's Counter and tuple unpacking mirror this exactly over the same n rows, so both are O(n), matching the label.", "label": "O(n)", "sid": "378_91", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-03", "cause": "label", "confidence": "high", "evidence": "`aSorted := SortInts(a_list)` sorts the whole seq of size m (merge sort, Theta(m log m) per the table) before a loop only reads its first `m`-param elements, plus three separate Theta(strLen) passes over `s`, so the tight class is Theta(strLen + m log m); n (string length) and m (the count read on its own input line) are independent inputs, not a naming split of one input, and Python's `sorted(map(int,...))` followed by the same linear passes is the same Theta(n + m log m), not O(n log n), so the label is wrong on the Python's own terms.", "label": "O(nlogn)", "sid": "380_112", "true_class": "O(n+mlogm)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "The array l has size n+1 and is zero-initialized in O(n); the second loop runs |a_list|=m times doing O(1) array writes each; the third loop runs strLen=n times, appending s[idx] or s[strLen-idx-1] to result with no read-back of result itself in that loop, so each append is O(1). Total is O(n+m), matching both the label and the Python's identical difference-array algorithm over the same n and m.", "label": "O(n+m)", "sid": "380_19", "true_class": "O(n+m)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "Reduce recurses once per removed petal (up to |petals| times), and each call does SumSeq (O(|petals|)), a min-scan loop (O(|petals|)), and RemoveFirst (a linear-recursive cons like ReplaceAll, O(|petals|)), so each level costs O(|petals|) and the whole recursion sums to O(n**2). The Python's per-iteration sum(), min(), and list.remove() are each O(len(petals)) too, over the same O(n) iterations, matching O(n**2).", "label": "O(n**2)", "sid": "384_34", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "SortInts costs O(k log k) per the table's merge-sort entry, dominating the O(n) SumSeq call and the O(n) linear scan for the first odd element, so the whole method is O(n log n). The Python's list.sort() (Timsort, O(n log n)) followed by O(n) sum() and an O(n) for-loop over the same list matches the same class.", "label": "O(nlogn)", "sid": "384_38", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-batch-03", "cause": "label", "confidence": "high", "evidence": "Corrected by review: the auditor called this translation because the Python uses max(dimensions[i]) where the Dafny reads row[0]/row[1]. A rectangle row holds exactly two numbers, so that max is O(1) per row and the Python is O(n) too. The O(n*m) label names a dimension neither source scans. Matches the proved bound 8*|rectangles| + 6 in solutions-verified/.", "label": "O(n*m)", "sid": "396_361", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "The loop reads only row[0] and row[1] from each of the n rectangles (a fixed two-column access consistent with 'requires |rectangles[k]|>=2') and does O(1) comparisons and swaps per iteration, so the cost is O(n) with no dependence on row width. The Python's identical w,h=map(int,...) parse and comparisons over the same n rows match O(n).", "label": "O(n)", "sid": "396_58", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "The loop runs exactly n times (bounded by 'requires 0<=n<=|arrows|') doing O(1) character comparisons and counter updates each iteration, matching the Python's identical for i in range(n) loop indexing s[i], so both are O(n).", "label": "O(n)", "sid": "410_105", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-03", "cause": "label", "confidence": "high", "evidence": "ContainsChar, FindFirst and FindLast are each linear recursions over `arrows` costing O(|arrows|), called to emulate Python's `in`, `.find()`, and `.rfind()`, which are themselves O(n) string scans in CPython; both the Python and the Dafny are Theta(n), so the O(1) label is wrong on both sides, not just in translation.", "label": "O(1)", "sid": "410_176", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "T's while loop decrements b0+b1 by exactly 1 per iteration until both reach 0, so each call to T costs O(initial b0+b1); Solve calls T(a-1,b,0) and T(a,b-1,1), each O(a+b)=O(n+m) with O(1) work per iteration, matching the Python's identical b[0]/b[1] decrement loop called twice on the same n and m.", "label": "O(n+m)", "sid": "433_16", "true_class": "O(n+m)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "Solve computes a max and a min via two ternary expressions and formats them with IntToString, with no loop or recursion anywhere, matching the Python's max()/min() one-liner -- both are O(1).", "label": "O(1)", "sid": "433_57", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "Solve takes |s| once and does one multiplication and subtraction before formatting the output string, with no loop or recursion, matching the Python's single print(len(input())*2-1) expression -- both are O(1).", "label": "O(1)", "sid": "450_204", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "IsPalinFrom recurses at most |s|/2 times, each level doing one O(1) character comparison at s[i] and s[j] before narrowing the window, so the whole check is O(n). The Python's isPalin while loop does the identical two-pointer scan over the same string in O(n), matching the label.", "label": "O(n)", "sid": "450_276", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-02", "cause": "", "confidence": "high", "evidence": "The while loop runs n-1 times doing 's := s + \"0\"' with no read-back of s via indexing anywhere in that loop, so each append is O(1) per the table's deferred-concat rule and the loop's cost is O(n) from loop overhead alone. The Python's identical for i in range(k-1) loop calling print('0') the same number of times matches O(n), with n taken as the value k.", "label": "O(n)", "sid": "457_27", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "medium", "evidence": "The while loop adds a growing power of nine to nn until NumDigits(nn) reaches k, and both nn and pow are bignums whose digit count grows roughly linearly with the loop index, so each addition and the final IntToString cost more than O(1); summed over the k-ish iterations this is quadratic in k, matching the label, and the Python performs the identical n+=9**i loop so it is not a translation-only effect.", "label": "O(n**2)", "sid": "457_38", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "Solve computes one division, one comparison-based min, and one addition over the two int parameters with no loop, no recursion, and no sequence access, exactly mirroring the Python's single arithmetic expression, so both are O(1).", "label": "O(1)", "sid": "459_146", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The while loop reduces n and m by a total of three per iteration in either branch until neither condition holds, so the iteration count is bounded by O(n+m); the Python's for loop over range(5*10**5) with the same decrement logic and identical break condition matches this bound exactly.", "label": "O(n+m)", "sid": "459_199", "true_class": "O(n+m)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The single while loop scans string_ once using string_[i] with O(1) work per character (two character comparisons and at most one assignment), giving O(|string_|)=O(n), and the Python for loop over N performs the same single pass.", "label": "O(n)", "sid": "493_134", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-04", "cause": "label", "confidence": "high", "evidence": "The while loop scans the entire string_ to find hasC and lastF, an O(n) pass equivalent to Python's s.index('C') and s.rindex('F'), which are themselves O(n) scans; O(1) is wrong for both the Dafny and the Python.", "label": "O(1)", "sid": "493_64", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The while loop walks a_list exactly once, computing Abs359 and updating answer/cnt_minus/cnt_0 in O(1) per element, giving O(|a_list|)=O(n); the Python for loop over a does the identical single pass with O(1) work per element.", "label": "O(n)", "sid": "499_359", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "SortInts(a_list) costs O(n log n) per the prelude's merge sort, and the following while loop advances by 2 over n elements doing O(1) work per pair, so the sort dominates at O(nlogn); the Python's a.sort() plus an identical stride-2 loop matches this.", "label": "O(nlogn)", "sid": "499_82", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "Resolved by review with the problem statement: 913_C caps the bottle-type count at 1 <= n <= 30, so Pow2_140(idx) is called with idx < 30 and its recursion depth is a constant. The feared sum over idx is bounded by a constant, the sort over n items dominates, and the O(nlogn) label stands.", "label": "O(nlogn)", "sid": "514_140", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-batch-04", "cause": "label", "confidence": "high", "evidence": "Every loop in Solve is bounded by the literal constant 31 (idxFill<31, i<30, j from 30 down to 0), not by n, which itself the requires clause caps at 31; Python's source uses the same fixed N=31 ranges regardless of n, so both are O(1), not O(n).", "label": "O(n)", "sid": "514_39", "true_class": "O(1)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The first while loop performs a := a[i := if a[i] < cand then a[i] else cand] every iteration, and each seq update is an O(n) copy, making that loop alone O(n**2); the second loop calls FCost90, whose ParseBinary90/LowSum90 recursions are each O(n), giving another O(n**2). The Python is independently O(n**2) too: f() reparses int(bs,2) and resums a generator over up to n-1 elements on every call, and this f() runs up to len(bx) times in the second loop, so the label is correct about the Python and the seq-update in loop one does not change the overall class, since loop two already dominates at O(n**2) in both languages.", "label": "O(n**2)", "sid": "514_90", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The single while loop scans points once, reading points[i][0] and points[i][1] (O(1) each) and updating count with O(1) arithmetic per iteration, giving O(n); the Python for loop over range(n) performs the identical single pass reading two ints per line.", "label": "O(n)", "sid": "525_234", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-04", "cause": "label", "confidence": "high", "evidence": "The while loop only ever reads points[i][0], points[i][1], and the previous row's same two indices, never walking a row of width m, so the code is O(n) not O(n*m); sibling row 525_234 with the identical read pattern is correctly labelled O(n), and Python's min(pair[i])/max(pair[i-1]) also only ever operate on the fixed two-element rows this problem produces.", "label": "O(n*m)", "sid": "525_273", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-04", "cause": "label", "confidence": "high", "evidence": "The while loop is bounded by the literal constant 5 (i<5), not by any size parameter n, and AllSame3456/IndexOf1From3456 run over a fixed-width row, so the whole method is O(1); Python's for i in range(0,5) loop with break is the same fixed 5-iteration scan, also O(1).", "label": "O(n)", "sid": "531_3456", "true_class": "O(1)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-04", "cause": "label", "confidence": "high", "evidence": "Solve's only loop runs i<5, a literal constant unrelated to any n, calling IndexOf1From3499 over a fixed-width row each time, so the method is O(1); Python's for i in range(5) loop calling input().split().index('1') is the same constant-bounded scan, with no sort or n-sized structure anywhere in either program.", "label": "O(nlogn)", "sid": "531_3499", "true_class": "O(1)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "Solve branches once on k%2 and evaluates a single division-based formula with no loop, no recursion, and no sequence access, exactly mirroring the Python's single conditional expression, so both are O(1).", "label": "O(1)", "sid": "540_100", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "medium", "evidence": "Each while loop moves na toward a by steps of 2, so the iteration count is bounded by O(n) given the requires clause 1<=k<=n; the Python's identical while loops with the same step-2 decrement/increment and the same n bound on a match this exactly, so both are O(n) as labeled.", "label": "O(n)", "sid": "540_80", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The while loop over pairs does O(1) work per test case via a constant-size tuple lookup table[nv-1] and RowField107's fixed 10-way branch, appending to results with no read of results in the same loop; the closing Join costs O(total output length)=O(n), matching the Python's constant-time a[n-1][k] lookup per test case.", "label": "O(n)", "sid": "554_107", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-04", "cause": "label", "confidence": "high", "evidence": "The two inner while loops are bounded by 100-K and K+1, and the requires clause caps K at 0..9 and N at 1..150, so per-query work (DigitSum, RepeatChar, ParseDecimal) is a fixed constant, not proportional to any n; the outer loop over pairs is then O(n) in the number of test cases, not O(n**2); Python's identical range(100-K)/range(i,i+K+1) loops under the same N<=150,K<=9 bound are equally constant per query.", "label": "O(n**2)", "sid": "554_70", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "SplitChar recurses once per character of s, each level doing O(1) work with no s[i] reads inside the concatenation, giving O(|s|); the outer while loop over the resulting pieces does O(1) work per piece via Min and modulo, so the total is O(n), matching the Python's single loop over lens.", "label": "O(n)", "sid": "566_101", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "SplitChar is O(|s|)=O(n) as in its sibling 566_101, and Sort(lens, LessInt) costs O(n log n) per the prelude's merge sort; the following while loop over row does O(1) work per element via Min, so the sort dominates at O(nlogn), matching the Python's sorted(..., key=len) call.", "label": "O(nlogn)", "sid": "566_183", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "medium", "evidence": "The check-building loop terminates once check's last element exceeds the hardcoded constant 1000000000, a bound independent of n or the query values, so its cost (even with the O(|check|) read-then-append pattern the facts flag) is a fixed constant, not a function of n. The outer loop over numbers_list then calls Dfs once per query, and FindIdxFrom scans the same fixed-size check array, so each query costs a constant relative to n, giving O(n) overall; the Python's identical precompute-then-dfs structure matches. The code comment notes three stored inputs exceed the intended 1..1e9 range, but Dfs still terminates using the largest available check value, adding only a small constant number of extra steps for those cases, not a new asymptotic dependency on n.", "label": "O(n)", "sid": "577_509", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "label", "confidence": "high", "evidence": "Resolved by review with the problem statement: 1345_B gives up to t <= 1000 test cases each with a value n <= 10^9. Pyramid is O(sqrt(value)), so a single query costs at most about 31623 steps regardless of how many queries there are. Total cost is linear in the query count, not quadratic.", "label": "O(n**2)", "sid": "577_656", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label reads as quadratic in the query count. Check the Input section: the per-test value n is capped at 10^9 independently of t, so the per-query sqrt work is a constant factor and the total is O(t). If instead the label's n means a single query's magnitude, the row is O(sqrt(n)) and still not quadratic. Sibling 577_509 carries O(n) for the same signature convention."}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "SortInts(numbers) costs O(n log n) per the prelude's merge sort, and the following while loop over n-1 pairs does O(1) arithmetic per pair, so the sort dominates at O(nlogn); the Python's l.sort() plus an identical linear scan matches this exactly.", "label": "O(nlogn)", "sid": "603_284", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The single while loop scans numbers once, comparing each element against running mn and mx with O(1) work per element, giving O(n); the Python's min(indices) and max(indices) each make a separate O(n) pass, so the overall Python cost is also O(n), matching the label.", "label": "O(n)", "sid": "603_489", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-05", "cause": "translation", "confidence": "medium", "evidence": "j.sort() in the Python runs on a list built by scanning i from 0 upward, so it is already in increasing order and costs O(n) under Timsort's best case, but h.sort() sorts the gap differences -j[f]+j[f+1]-1, which are not pre-ordered, so Python genuinely pays O(n log n) there and the label reflects that real sort. The Dafny drops sorting entirely (facts.sorts is empty) and tracks maxGap directly inside the single while loop over j, giving O(n), so the label is right about the Python and the translation is the defect.", "label": "O(nlogn)", "sid": "641_25", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "SplitByDot recurses/loops once per character of s building cur and parts by concatenation with no reads of cur or parts inside the loop, giving O(|s|); the outer while loop over segments does O(1) work per segment and can exit early via found, so the total is O(|s|)=O(n), matching the Python's split('.') plus a single scan over a.", "label": "O(n)", "sid": "641_31", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-03", "cause": "", "confidence": "high", "evidence": "The first while loop performs total := total[i := total[i+1]+a_list[i]] every iteration, an O(n) seq copy each time, making it O(n**2) by itself; the second loop similarly seq-updates both bob and ali every iteration, again O(n**2). The Python is independently O(n**2): sum(X[i:n]) slices and sums up to n-i elements fresh on every one of the n iterations, which is the real source of the labelled quadratic cost, so the label is correct about the Python and the Dafny's seq-update pattern lands on the same class rather than a worse one.", "label": "O(n**2)", "sid": "647_11", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "The Solve loop walks a_list once from the end doing O(1) max/addition per element, and the two IntToString calls only format the final s-x and x values, matching the O(n) label.", "label": "O(n)", "sid": "647_54", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "SortInts is called once on the full array giving O(n log n), and the follow-up while loop over `a` does O(1) work per index, matching the O(nlogn) label exactly as in the Python's a.sort() plus scan.", "label": "O(nlogn)", "sid": "659_63", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "Building `a` and `b` and the matching scan are linear in |s|, then SortInts runs on `c` (at most 2|a| elements) contributing O(n log n); CountChar661 is declared but never invoked so it does not add cost.", "label": "O(nlogn)", "sid": "661_47", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "Two linear scans via CountChar661b(s,'(') and CountChar661b(s,')') followed by the bracket-counting while loop are each O(|s|) with O(1) per character, matching the O(n) label.", "label": "O(n)", "sid": "661_99", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "medium", "evidence": "Each string t is scanned by two recursive CountChar calls (s[0]/s[1..] descent) and by Repeat(\"01\",|t|), so total cost tracks the sum of the binary_strings' lengths, matching O(n) under the total-input-size convention used elsewhere in this dataset; this assumes CountChar's slice recursion is linear like the prelude's SumSeq/MaxSeq rather than quadratic.", "label": "O(n)", "sid": "662_527", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-05", "cause": "label", "confidence": "low", "evidence": "IsAllChar and the Repeat(\"01\", nn)/Repeat(\"10\", nn) calls are each O(|s|) per the cost table's explicit Repeat rule, so the Dafny is linear in total string length rather than quadratic. Python's `ans = ans + \"01\"` loop is the classic in-place string-concatenation pattern, which CPython's refcount-1 optimization typically makes amortized linear rather than the naively-assumed O(n**2), so the label may be wrong on the Python side too rather than only on the translation; I am not fully certain this optimization reliably applies here.", "label": "O(n**2)", "sid": "662_559", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-05", "cause": "translation", "confidence": "high", "evidence": "The Dafny never calls SortInts (facts.sorts is empty) and instead finds mn/mx and then counts occurrences of mn/mx in two separate O(n) passes, giving O(n) total. Python's s.sort() runs on the raw, arbitrary-order list s before min/max are taken via a=min(s), b=max(s), a real O(n log n) cost that the label reflects, so the omitted sort is the translation's dropped cost.", "label": "O(nlogn)", "sid": "669_107", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "A single O(n) scan over a_list updates mx/mn/maxf/minf with O(1) comparisons per element, and the two closing IntToString calls format bounded results, matching the O(n) label.", "label": "O(n)", "sid": "669_40", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "The while loop runs from i=1 to mn=min(a,b), a raw integer value from the signature rather than a sequence length, and the code returns early for |a-b|>1, so the O(n) label describes this value-bounded loop correctly.", "label": "O(n)", "sid": "681_105", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-05", "cause": "label", "confidence": "high", "evidence": "FactorialMod681 is called twice, each running a while loop `i <= n` for n or m iterations, so the Dafny scales linearly with the scalar inputs rather than being constant. Python's math.factorial(n) and math.factorial(m) compute a genuinely growing product, which the cost table's own rule flags as not O(1) for factorial-like operations, so the O(1) label undercounts both sides.", "label": "O(1)", "sid": "681_52", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-05", "cause": "translation", "confidence": "high", "evidence": "Max685 is a plain O(n)/O(m) linear scan with no SortInts call anywhere (facts.sorts is empty), so the Dafny is O(n+m). Python calls sorted(int_array_a) and sorted(int_array_b) purely to then read int_array_a[-1] and int_array_b[-1], a real O(n log n)+O(m log m) cost that the label reflects, so the translation dropped the sort that Python actually pays for.", "label": "O(nlogn+mlogm)", "sid": "685_583", "true_class": "O(n+m)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "MaxSeq is called once on list1 and once on list2, each O(length) per the prelude table, with no other loop present in Solve, giving O(n+m) exactly as labelled.", "label": "O(n+m)", "sid": "685_777", "true_class": "O(n+m)", "verdict": "ok"}
{"auditor": "labelaudit-batch-05", "cause": "label", "confidence": "medium", "evidence": "The while loop implements a continued-fraction search (p0/q0/p1/q1, k := FloorDiv(n,d)) whose iteration count is the number of continued-fraction terms of x0/y0, an O(log(y0)) Euclidean-style bound rather than a fixed constant; Python's Fraction(x,y).limit_denominator(n) performs the same continued-fraction expansion internally, so O(1) undercounts both sides, though I am not fully certain the dataset doesn't intend O(1) loosely here.", "label": "O(1)", "sid": "696_49", "true_class": "O(logn)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "medium", "evidence": "The while loop runs b from 1 to n, a value taken directly from v_2 with no seq argument in the signature, doing O(1) FloorDiv/AbsInt arithmetic per iteration, matching the O(n) label under the value-bounded-loop convention.", "label": "O(n)", "sid": "696_51", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "timeToSolve is capped at 240 by problem constraints on b, so prob is bounded by a small constant and the while loop on `fuel` runs at most about fifty times regardless of a or b, making the method O(1) as labelled.", "label": "O(1)", "sid": "704_351", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "The while loop runs from i=1 to a, a raw integer from the signature, doing O(1) multiplication, addition and comparison per iteration, matching the O(n) label.", "label": "O(n)", "sid": "704_614", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-06", "cause": "label", "confidence": "medium", "evidence": "The loop over numbers calls Repeat(\"9\",cnt9) and Repeat(\"8\",cnt8), each costing O(length) per the table, so total cost is linear in the summed test values with no nested or squared construct; the Python's `['9']*(n-...) + ['8']*...` list construction is the same linear build, so O(n**2) matches neither source.", "label": "O(n**2)", "sid": "721_169", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-06", "cause": "label", "confidence": "medium", "evidence": "The single loop over numbers builds each output line with two Repeat calls costing O(length) and there is no Sort, log, or nested-loop construct anywhere in this Dafny or the Python, so the tight cost is linear in the summed test sizes, not O(nlogn).", "label": "O(nlogn)", "sid": "721_309", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "aArr and bArr are each sorted once via SortInts giving O(n log n), and the two outer/inner while pairs together advance the index i only |aSorted| or |bSorted| times total (amortised O(n)), so the whole method is O(n log n) as labelled.", "label": "O(nlogn)", "sid": "750_14", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "The first loop is O(n) over data_points doing O(1) FloorMod and array writes to aArr/bArr; the second loop runs a fixed 2001 iterations, a constant, so the whole method is O(n) as labelled.", "label": "O(n)", "sid": "750_51", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "Two O(n) loops, one over numbers writing into tmp and one scanning tmp[0..n], each do O(1) array reads/writes per iteration, matching the O(n) label.", "label": "O(n)", "sid": "785_12", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "Sort is invoked once on the n (numbers[i], i+1) pairs giving O(n log n) per the prelude table, and the remaining three while loops over dp/q are each O(n) with O(1) array work, matching the O(nlogn) label.", "label": "O(nlogn)", "sid": "785_75", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-batch-06", "cause": "label", "confidence": "medium", "evidence": "The trial-division loop `while i < nv` and the three pointer-advancing loops all share the single decrementing counter cnt initialised to nv, so each test case costs O(nv), not O(nv**2); the Python's identical `for i in range(1,n)` plus three shared-counter while loops is the same linear structure, so O(n**2) matches neither source.", "label": "O(n**2)", "sid": "794_215", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "medium", "evidence": "Per test case the arr-building, best-selection and vals-building while loops are each O(nn) (appends deferred with no read in the same loop, one flatten per later scan), so total cost is O(sum of nn across the |data| test cases), matching O(n) under the total-input-size convention.", "label": "O(n)", "sid": "794_794", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-06", "cause": "label", "confidence": "medium", "evidence": "The triple nested while loops over i<=nv/3, j<=nv/5, k<=nv/7 are all bounded by the same per-test value nv, giving worst-case O(nv**3) per test, which is cubic and outside the O(n**2) label; the Python's identical triple `for` loop with the same early-break `f==1` guard is equally cubic, so the label is wrong for both sources.", "label": "O(n**2)", "sid": "795_300", "true_class": "other", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "The while loop runs |numbers| times doing O(1) modulo/division arithmetic and a deferred string concat per iteration (no read forcing a flatten inside the loop), matching the O(n) label.", "label": "O(n)", "sid": "795_580", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "medium", "evidence": "The while loop runs n_edges times reading edges[i][0] and edges[i][1] (each row's width is pinned to at least 2 by the requires clause) doing O(1) work per iteration, matching the O(n) label where n denotes the edge count.", "label": "O(n)", "sid": "804_10", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "medium", "evidence": "The outer loop runs |pairs| times; IntSqrt does a binary search bounded by log2(a*b+1), which is capped by the fixed bit-width of a machine integer regardless of the query count, so each query is O(1) and the method is O(n) as labelled, mirroring Python's O(1) math.sqrt call.", "label": "O(n)", "sid": "810_131", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-06", "cause": "label", "confidence": "low", "evidence": "Check's binary search `while l+1<r` over r-l is bounded by the value b-a, not by the query count n, so like sibling row 810_131 the per-query cost is effectively constant and the total across |pairs| queries is O(n), not O(nlogn); the Python's identical binary search over the same b-a range shows the same pattern.", "label": "O(nlogn)", "sid": "810_98", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-04", "cause": "", "confidence": "high", "evidence": "The while loop is capped at 9 iterations by the `i < 9` guard regardless of |word|, and each iteration's slice-and-concat s[..i]+\"A\"+s[i..] costs at most O(|s|), so the method is O(|word|) overall, matching Python's bounded S.insert calls; both are O(n) as labelled.", "label": "O(n)", "sid": "831_184", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The four while loops each range over the fixed constant 0..1 (i1..i4), producing exactly 16 candidate strings regardless of |word|, and each `cand == word` compares against a candidate string whose length is capped at 13 characters, so total work is bounded independent of n.", "label": "O(1)", "sid": "831_74", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The single while loop advances i and j under the lexicographic decreases (n - i, |d| - j), and p/j together are bounded by 2n, so the two-pointer body's O(1) work reading d[j], d[i] and evaluating SumProg runs a total of O(n) times.", "label": "O(n)", "sid": "888_179", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "ReverseSeq(doubled) recurses on s[1..] and concatenates + [s[0]] at every level, copying O(current length) per level for a total of O(n**2); the Python's D[::-1] slice reversal and the surrounding two-pointer scan are all O(n), so the label matches the Dafny only by coincidence.", "label": "O(n**2)", "sid": "888_6", "translation_defect": true, "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "Both while loops scan a_list once each over 0..|a_list|, reading a_list[idx] and accumulating a running sum or prefix sum with O(1) work per iteration, matching the Python's single for-loop pass over cs.", "label": "O(n)", "sid": "894_3", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-07", "cause": "translation", "confidence": "high", "evidence": "Python's Valid(l,i,x,y) recomputes prefix sums and calls sum(l) from scratch for each i up to the break point, giving the labelled O(n**2); the Dafny instead tracks s and total incrementally in a single pass with a found flag, doing O(1) work per iteration of the while loop, so it is O(n).", "label": "O(n**2)", "sid": "894_85", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "Solve has no loop or recursion at all; ans := number/5, r := number%5, and the single if-branch are constant-count arithmetic operations, exactly mirroring the Python's one-shot // and % computation.", "label": "O(1)", "sid": "897_1880", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The while loop decrements x by up to 5 per pass (decreases x) so it runs O(x) times; number is the only scalar input (capped at 1e6 in the statement) and stands in for n here, making this an O(n) loop, matching the Python's identical decrement loop.", "label": "O(n)", "sid": "897_2438", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-07", "cause": "harness", "confidence": "high", "evidence": "The Dafny Solve only reads row[0] and row[2] from each grid row inside the while loop and never walks the row, so it is O(n); Python's L=list(map(int,input().split())) additionally pays to parse each full line, which is outside what Solve measures, so O(n*m) is right about the harness, not the method.", "label": "O(n*m)", "sid": "936_203", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The single while loop over grid reads exactly row[0..3], four O(1) index reads plus O(1) IntToString calls on bounded-magnitude integers, per iteration, so cost scales with |grid|=n, matching the Python's one-pass loop over q queries.", "label": "O(n)", "sid": "936_772", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The while loop halves m every iteration (m := m/2) until m==1, so it runs O(log n) times where n is the initial value, mirroring the Python's identical `n = n//2` halving loop.", "label": "O(logn)", "sid": "945_255", "true_class": "O(logn)", "verdict": "ok"}
{"auditor": "labelaudit-batch-07", "cause": "label", "confidence": "high", "evidence": "The Dafny while loop halves m each iteration until m==0, taking O(logn) iterations; Python's bin(x) produces a string of length proportional to the bit-length of x and .count('1') scans it, also O(logn), so the labelled O(1) is wrong for both, not just the translation.", "label": "O(1)", "sid": "945_880", "true_class": "O(logn)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-05", "cause": "translation", "confidence": "medium", "evidence": "ReverseInts(arr) recurses via `ReverseInts(s[1..]) + [s[0]]`, the identical slice-and-concatenate-at-every-level shape flagged for ReverseSeq in 888_6, which copies O(current length) per level; since |arr| can be as large as n (a fully increasing run of a_list), this call alone is O(n**2), while the Python builds the answer with O(1) `arr.append` calls and a single O(n) `arr[::-1]` slice.", "label": "O(n)", "sid": "952_116", "translation_defect": true, "true_class": "O(n**2)", "verdict": "mismatch", "what_to_look_for": "The label assumes the final reversal costs O(n) the way Python's arr[::-1] does. Open ReverseInts and confirm it is defined as `ReverseInts(s[1..]) + [s[0]]`; if the backend does not special-case that concatenation the way it does a flat loop's `s := s + [x]`, each level copies the accumulated result and the call is O(|arr|**2). Also check how large |arr| can get -- if a single test can make a_list one long increasing run, |arr| ~= n and the quadratic term dominates, so the label is too low; if |arr| is provably small for every valid input, the label could still stand."}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "medium", "evidence": "SortInts(a_list) costs O(n log n) and dominates the remaining single-pass array loops (whereIs, dp, preState, lst, resArr all use `new int[n]` with O(1) index writes); the rankMap map<int,int> is inserted into n times, and under any ordinary map implementation (O(1) amortised or O(log n) worst case) that does not exceed the sort's term, so the total stays O(n log n).", "label": "O(nlogn)", "sid": "952_163", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-batch-07", "cause": "label", "confidence": "high", "evidence": "Solve requires 1 <= n <= 4, so the while loop over n and the MaxSeq/MinSeq calls over xs/ys all run over a problem-capped constant-size input; per the value-bounded-loop rule this is O(1) in both Python and Dafny, so the labelled O(n) is wrong for the label, not the translation.", "label": "O(n)", "sid": "966_134", "true_class": "O(1)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-05", "cause": "label", "confidence": "high", "evidence": "The problem statement caps n at 4 (`1 <= n <= 4`, the surviving corners of a rectangle), so the nested loop `while i<n {... while j<n ...}` in Solve executes at most 4*3/2=6 total inner-body evaluations for any valid input rather than a quantity that grows with n; the Python's identical double loop over s is bounded the same way.", "label": "O(n**2)", "sid": "966_54", "true_class": "O(1)", "verdict": "mismatch", "what_to_look_for": "The label O(n**2) implies the double loop's cost grows without bound as n increases. Reread the problem statement's Input section and confirm n really is capped at 4 (the vertex count of a rectangle) rather than being a general array-size n; if the cap is genuine and fixed across all valid inputs, both loops run a bounded number of times and the true class is O(1), not O(n**2). If some other reading of the statement lets n scale further, the label stands."}
{"auditor": "labelaudit-batch-07", "cause": "translation", "confidence": "medium", "evidence": "The second while loop removes one element per iteration via keys := keys - {k}, a set-difference rebuild that costs O(|keys|) like the documented set-insert cost, so the loop sums to O(n**2); Python's for a in d dict iteration is O(n), matching the labelled O(n).", "label": "O(n)", "sid": "976_1131", "true_class": "O(n**2)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The nested while loops (i over |a_list|, j over |a_list|) count occurrences with O(1) work per (i,j) pair, giving O(n**2); the Python's `numArray.count(x)` inside its own O(n) for-loop is O(n) per call for n calls, so both sides are quadratic.", "label": "O(n**2)", "sid": "976_656", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-batch-07", "cause": "translation", "confidence": "high", "evidence": "The while loop `(f+1)*(f+1) <= n` increments f from 0 to about sqrt(n), an O(sqrt(n)) construct that Python does not pay for since rt = n**0.5 is a single hardware float operation, O(1); the true Dafny class is O(sqrt(n)), outside the given vocabulary, not the labelled O(1).", "label": "O(1)", "sid": "1011_177", "true_class": "other", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-07", "cause": "label", "confidence": "high", "evidence": "The while loop `a*b < n` alternately increments the smaller of a and b, so both a and b converge to about sqrt(n) and the loop runs O(sqrt(n)) times; Python implements the identical loop, so both languages are O(sqrt(n)), not the labelled O(n), which is a label error.", "label": "O(n)", "sid": "1011_368", "true_class": "other", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-07", "cause": "label", "confidence": "medium", "evidence": "When m>=1 (n odd) the seq(2*m, ...) comprehension builds a sequence of length 2m, O(n) work that the loop-counting facts miss because it is a comprehension, not a while loop; Python's generator unpacked into print over range(2*n) does the same O(n) work, so the labelled O(1) is wrong for both.", "label": "O(1)", "sid": "1015_129", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The single while loop from i=1 to n builds partA and partB via `seq + [x]` with no read in that loop (O(1) each), and the one concatenation `partA + partB` plus the single JoinInts call happen once outside the loop, so the whole method is O(n), matching the Python's identical single pass.", "label": "O(n)", "sid": "1015_168", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The while loop runs n times reading exactly edges[2i][0..1] and edges[2i+1][0..1] (four O(1) index reads) per iteration and appends one line with no read in that loop, and Join(lines, \"\\n\") is a single O(total length) call outside the loop, giving O(n) overall, matching the Python's one loop over test cases.", "label": "O(n)", "sid": "1018_254", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-batch-07", "cause": "label", "confidence": "high", "evidence": "The inner while loops over i and j are hardcoded to 2 iterations each (`i < 2`, `j < 2`), never scanning a variable-width row, so there is no m dimension in either the Dafny or Python (`for i in range(2): for j in range(2)`); the true class is O(n), and the labelled O(n*m) is a label error.", "label": "O(n*m)", "sid": "1018_421", "true_class": "O(n)", "verdict": "mismatch"}
{"auditor": "labelaudit-batch-07", "cause": "translation", "confidence": "high", "evidence": "The Dafny dedups target values with a nested while loop (`while j < i`) comparing every new entry against all previous entries via signs/nums/dens, an O(n**2) construct with no SortInts call at all; Python instead does target.sort() then itertools.groupby, giving the labelled O(nlogn), so the translation replaced the sort with quadratic pairwise comparison.", "label": "O(nlogn)", "sid": "1027_141", "true_class": "O(n**2)", "verdict": "mismatch"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The outer loop over d_list runs n times and its inner loop scans the accumulated dxs/dys arrays (`while j<|dxs|`), so building the deduplicated direction list is O(n**2) in the worst case; the Python does the same linear scan through the set d (`for x1,y1 in d`) for each of n points, so both are quadratic and the label matches.", "label": "O(n**2)", "sid": "1027_207", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "translation", "confidence": "high", "evidence": "This sibling of 1027_207 keeps the same nested-loop dedup: `while j<|dxs|` scans the whole accumulated slope list for each of the n points, an O(n**2) construct; but its Python replaces the linear scan with `if m not in slope` on a dict keyed by slope, an O(1)-average membership test, making the Python genuinely O(n) as labelled.", "label": "O(n)", "sid": "1027_458", "translation_defect": true, "true_class": "O(n**2)", "verdict": "mismatch", "what_to_look_for": "The label O(n) is claimed to match the Dafny, but check whether the inner `while j < |dxs|` loop is bounded by a constant or by the growing list dxs; if dxs can hold up to n distinct slopes (e.g. all points at different angles from the gun), that inner loop's total work across all outer iterations is O(n**2), the same shape as 1027_207's Dafny. Compare directly against 1027_207's inner loop, translated the same way and already labelled O(n**2) -- if the two Dafny loop bodies are structurally identical, one of the two labels must be wrong, and the Python source (dict membership vs manual scan) settles which one."}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The two separate while loops scan a_list and b_list once each, and BitOr recurses by halving both operands each call so its depth is bounded by the fixed bit-width of values up to 1e9 (a constant), giving O(n+m) overall, matching the Python's two O(n) and O(m) accumulation loops.", "label": "O(n+m)", "sid": "1029_119", "true_class": "O(n+m)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The nested while loops (l over 0..n, r over l..n) evaluate BitOr once per (l,r) pair with O(1) cost per call (bounded recursion depth from the fixed value magnitude), so the total is O(n**2), matching the Python's identical double loop over l and r.", "label": "O(n**2)", "sid": "1029_92", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-05", "cause": "translation", "confidence": "high", "evidence": "firstPos := firstPos[x := i] and lastPos := lastPos[x := i] are seq updates (`s[i:=v]`) executed inside loops over |nums|=n elements; each seq update on a length-(n+1) seq copies the whole sequence, turning two O(n) loops into O(n**2), while the Python performs the equivalent first_pos[x]=i and last_pos[x]=i as O(1) in-place list assignments, keeping the Python at the labelled O(n log n) via last_pos.sort() and n bisect calls.", "label": "O(nlogn)", "sid": "1039_15", "translation_defect": true, "true_class": "O(n**2)", "verdict": "mismatch", "what_to_look_for": "The label assumes firstPos/lastPos updates are O(1) as Python list assignment is. Open the two build loops and confirm the update is `firstPos := firstPos[x := i]` / `lastPos := lastPos[x := i]` on a seq<int> rather than a mutable array<int>; if it is a functional seq update, each call copies n+1 entries and the two loops are quadratic, so the Dafny (not the Python) needs the fix -- switching firstPos/lastPos to array<int> would restore O(n log n)."}
{"auditor": "labelaudit-r2-05", "cause": "", "confidence": "high", "evidence": "The loop over a_list calls Repeat(\"3\", v-1) whose cost is O(v), but the problem statement guarantees the sum of v across all test cases is at most 1e5, so the total work across the whole loop is bounded by that same sum, matching the Python's identical per-test \"3\"*(v-1) string construction and the labelled O(n).", "label": "O(n)", "sid": "1043_358", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "medium", "evidence": "The loop over a_list (t=n test cases) calls Repeat(\"3\", m-2) whose argument is the per-test digit count m, capped by the problem's stated bound that the sum of m across all test cases never exceeds 1e5, so this per-test cost is bounded independent of n and the total is O(n), not O(n**2); the Python's identical while-loop over the same capped m shows the same growth.", "label": "O(n**2)", "sid": "1043_408", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label assumes total print work scales with n**2, as if the number of test cases and the per-test digit value both grow together. Check the description's line 'sum of n for all test cases does not exceed 1e5': since that caps the Repeat/print cost independent of |a_list|, increasing the number of test cases while holding that sum fixed should not increase total work quadratically. If a reviewer can construct a valid input where both the test-case count and the total digit-sum grow together without violating that cap, the O(n) verdict is wrong instead."}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Single while loop over |strings|=n cells, each cell string bounded to a small constant length by the problem's column/row cap of 10^6; DigitRunLen, ParseInt and NumToLetters recurse over that bounded cell length, so total cost is O(n), matching the label.", "label": "O(n)", "sid": "1047_26", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "high", "evidence": "Each cell string a is bounded to a small constant length because columns and rows are capped at 10^6 by the problem, so IsRCFormat, DigitRunEnd, the alpha-scan loops, and ColToLetters all do O(1) work per cell; the outer loop over |strings| test cases is the only scaling factor, giving O(n), matching sibling 1047_26's identical O(n) treatment of the same problem.", "label": "O(n**2)", "sid": "1047_641", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label O(n**2) implies either a nested scan over n test cases or per-cell work that grows with n. Open the Dafny and check that DigitRunEnd, ColToLetters and the alpha-scanning while loops only ever iterate over a single cell string a, whose length is bounded by the problem's 10^6 column/row cap (at most a handful of characters), never by |strings|. If so there is no quadratic construct and the label is wrong; compare against sibling 1047_26, which solves the identical problem and is labeled O(n)."}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Three separate while loops each iterate exactly N+1 or |a_list| times, using array element assignment l[c] := ... which is O(1) per the cost table, so the total cost is O(N), matching the O(n) label and the Python's equivalent list-index assignment loop.", "label": "O(n)", "sid": "1053_38", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Solve builds a pair sequence of length N and calls Sort on it, which the cost table charges O(k log k), dominating the single O(N) scan that follows, so the tight class is O(n log n), matching the label and the Python's own list.sort() call.", "label": "O(nlogn)", "sid": "1053_44", "true_class": "O(nlogn)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "medium", "evidence": "Solve computes four Factorial(a) calls whose recursion multiplies a growing bignum accumulator across O(n) levels; per the cost table's rule that factorial-style int ops are not O(1), this bignum-growth cost is conventionally O(n**2), matching the label and the identical Fact() loop in sibling 1077_84 of the same problem.", "label": "O(n**2)", "sid": "1073_645", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "medium", "evidence": "The Dafny loop `while i < rawN { fact := fact * i; ... }` repeatedly multiplies a growing bignum accumulator by i, and per the cost table int operations whose value grows with n (factorials) are not O(1); the identical `fact *= i` pattern appears in sibling 1073_645/1077_84's Factorial function, both labeled O(n**2), so this row's O(n) label undercounts the same bignum-growth pattern.", "label": "O(n)", "sid": "1073_822", "true_class": "O(n**2)", "verdict": "mismatch", "what_to_look_for": "The label treats each `fact := fact * i` as an O(1) machine-word multiply, but per the cost table's 'factorials... not O(1); bignum' rule the multiplicand grows with i, so total cost should track the other two factorial-computing rows in this same problem (1073_645, 1077_84), both labeled O(n**2). Compare this row's single accumulation loop against those two Factorial-recursion rows; if a reviewer judges the growing-digit cost genuinely doesn't apply here (e.g. because n<=20 keeps the accumulator inside machine-word range throughout), the O(n) label should stand instead."}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Solve has no loops and no recursion, only a fixed sequence of arithmetic operations (FloorDiv, multiplication) on n regardless of its value, so the cost is O(1) as labeled, matching the Python's equally straight-line closed-form expression.", "label": "O(1)", "sid": "1077_64", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "medium", "evidence": "Solve computes four Factorial(a) calls that build a bignum product across O(n) recursive multiplications, matching the same bignum-growth convention used in sibling 1073_645 of the same problem, which is also labeled O(n**2), and the Python's equivalent while-loop Fact() function.", "label": "O(n**2)", "sid": "1077_84", "true_class": "O(n**2)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Solve is a single conditional expression comparing n-a and b+1 with no loop or recursion, so the cost is O(1) regardless of the values of n, a, b, matching the Python's one-line min() computation.", "label": "O(1)", "sid": "1089_174", "true_class": "O(1)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Solve is a single while loop iterating i from 0 to n with O(1) comparisons and an increment per iteration and no nested loop or recursion, so the tight cost is O(n), matching the Python's equivalent for-loop over range(n).", "label": "O(n)", "sid": "1089_96", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "high", "evidence": "Solve reads only row[0], row[1], row[2] from each matrix entry (a, b, c) per the `requires |matrix[k]| >= 3` precondition, and the triple-nested i/j/k loop ranges only over the fixed constants -1..1 (27 total iterations per test case), so row width m never enters the cost and the true class is O(n) where n=|matrix|, exactly mirroring the O(n*m)-on-fixed-row-width example in the audit guide.", "label": "O(n*m)", "sid": "1134_120", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label O(n*m) claims the cost scales with row width m, but check that the Dafny reads only row[0..2] (a, b, c) via the `requires |matrix[k]| >= 3` precondition, and that the i/j/k loops each range over the constant [-1,1] (27 total iterations), never over the row itself. If a reviewer finds any construct that walks the full row rather than three fixed indices, the O(n) verdict is wrong; otherwise, since the Python's own triple `for i in range(-1,2)` loops are equally fixed at 27 iterations, the label should be O(n)."}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "The single while loop runs |matrix| times, and MinSeq/MaxSeq are each called on a fixed 3-element sequence [a,b,c] costing O(1) per the cost table's 'O(length of argument)' rule, so total cost is O(n), matching the label and the Python's min/max over a 3-tuple.", "label": "O(n)", "sid": "1134_928", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Solve is a single while loop over n rows of `data`, reading only data[i][0] and data[i][1] with O(1) FloorMod and arithmetic per iteration and no nested scan, so the tight cost is O(n), matching the label and the Python's equivalent single for-loop.", "label": "O(n)", "sid": "1138_66", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "high", "evidence": "The single while loop iterates over n rows of `data`, reading only data[i][0] and data[i][1] (and data[i-1][1]) with O(1) FloorMod and arithmetic per iteration; the parameter m is the skip value x used only as a modulus operand, never as a loop bound or a scanned dimension, so the true class is O(n), matching sibling 1138_66's identical O(n) label for the same problem.", "label": "O(n*m)", "sid": "1138_83", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label O(n*m) implies the loop's cost scales with m, but m here is the skip-length x (a `requires m > 0` scalar), used only inside `FloorMod(..., m)` as an O(1) arithmetic operand, never as a range or size that gets walked. Confirm no loop in the Dafny iterates 'm times' or over a structure of length m; if none exists, compare to sibling 1138_66 (same problem, same structure, labeled O(n)) to confirm O(n) is correct here too."}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Solve is a single while loop scanning the string s once with O(1) character comparison and arithmetic per iteration, no nested loop or recursion, so the tight cost is O(n), matching the label and the Python's equivalent single for-loop over range(len(s)).", "label": "O(n)", "sid": "1168_24", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "high", "evidence": "The Dafny is a single while loop over `i < n` scanning the string s once with O(1) character comparisons, increments and a constant-time flag update per iteration; there is no nested loop, no seq_update_in_loop, and no recursion, so the tight class is O(n), exactly matching sibling 1168_24's O(n) label for the very same bracket-sequence problem.", "label": "O(n**2)", "sid": "1168_57", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label O(n**2) implies some nested or repeated pass over the string, but the Dafny body (facts: loop_depth 1, loops 1, no seq_update_in_loop, no set_build_in_loop) is a single pass with O(1) work per character. Check the loop body for any hidden seq copy (e.g. a slice or `s[i := v]`) that would make an iteration cost O(n) instead of O(1); absent that, compare to sibling 1168_24, which solves the same problem with the same loop shape and is labeled O(n)."}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "high", "evidence": "Each matrix row is read only at fixed indices l[0]..l[3] (A, B, C, D) per the `requires |matrices[i]| >= 4` precondition, with O(1) arithmetic and string selection per test case and no loop over row width; the outer loop runs |matrices|=n times, so the true class is O(n), and sibling 1170_62 solving the identical problem is correctly labeled O(n).", "label": "O(n*m)", "sid": "1170_10", "true_class": "O(n)", "verdict": "mismatch", "what_to_look_for": "The label O(n*m) implies cost scaling with row width m, but check that the Dafny only ever indexes l[0], l[1], l[2], l[3] -- four fixed positions -- and never loops over the row itself. If confirmed, m does not enter the cost and the label should be O(n), matching sibling 1170_62 which solves the identical 'Huge Boxes of Animal Toys' problem with the same structure and that label."}
{"auditor": "labelaudit-r2-06", "cause": "", "confidence": "high", "evidence": "Solve's single while loop runs |matrices| times, reading only the four fixed indices l[0]..l[3] per row with O(1) arithmetic and string selection, so the tight cost is O(n), matching the label and the Python's identical fixed four-tuple unpacking per test case.", "label": "O(n)", "sid": "1170_62", "true_class": "O(n)", "verdict": "ok"}
{"auditor": "labelaudit-r2-06", "cause": "label", "confidence": "high", "evidence": "Solve runs two separate while loops, one scanning `rectangles` for n iterations and a second scanning `checks` for m iterations, each doing O(1) min/max updates per iteration; n and m are independent parameters (each bounded separately at 200,000 in the problem statement), so the tight class is O(n+m), and the Python's own two separate for-loops over n and m show the identical two-phase linear cost.", "label": "O(n)", "sid": "1177_230", "true_class": "O(n+m)", "verdict": "mismatch", "what_to_look_for": "The label O(n) omits the second while loop, which scans `checks` (length m) exactly once with the same O(1) per-iteration cost as the first loop over `rectangles`. Check that n and m can vary independently per the problem's two separate bounds (1<=n<=200000 on one line, 1<=m<=200000 on another); if so the true class must include both dimensions as O(n+m), not O(n) alone."}
