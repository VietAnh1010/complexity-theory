{"sid": "1177_9", "label": "O(nlogn)", "verdict": "mismatch", "true_class": "O(nlogn+mlogm)", "cause": "label", "confidence": "high", "what_to_look_for": "The label O(nlogn) claims cost depends only on n, but Sort(checks,...) sorts p, the m-sized array, too. Check the second Sort call on checks/p and confirm the Python's p.sort() likewise costs O(mlogm); if so the m term must appear.", "evidence": "Solve sorts both `rectangles` (size n) and `checks` (size m) via two separate Sort calls, each O(k log k) per the table, and the Python mirrors this with c.sort() and p.sort(), so the true cost is O(nlogn+mlogm), not O(nlogn) alone.", "auditor": "labelaudit-batch-07"}
{"sid": "1180_39", "label": "O(n+m)", "verdict": "mismatch", "true_class": "O(n*m)", "cause": "translation", "confidence": "high", "what_to_look_for": "The label assumes the Dafny keeps Python's bisect binary search. Open the inner `while j < |x|` loop nested inside the loop over b_list and confirm it scans all of x linearly per query rather than calling a binary-search helper; if so every one of the b queries costs O(a), giving O(a*b).", "evidence": "For each of the b queries the Dafny runs a nested `while j < |x|` that scans the whole prefix-sum array x (length a+1) linearly, replacing Python's `bisect(x, v)` binary search, so the true cost is O(n*m) while the Python stays O(n + m log n), labelled O(n+m).", "translation_defect": true, "auditor": "labelaudit-batch-07"}
{"sid": "1180_626", "label": "O(n+m)log(n+m)", "verdict": "mismatch", "true_class": "O(n**2)", "cause": "translation", "confidence": "high", "what_to_look_for": "The label assumes `dorms[d_left] += dorms[d_left-1]` is an O(1) in-place update as it is in Python. Open the else-branch of the main while loop and confirm `dorms := dorms[d_left := ...]` is a seq update rather than an array write; if so it copies the whole length-a sequence up to a-1 times.", "evidence": "The else branch does `dorms := dorms[d_left := dorms[d_left] + dorms[d_left-1]]`, a seq update costing O(a) per the cost table, executed up to a-1 times as d_left advances, giving O(a**2) total versus the Python's O(1) in-place list assignment.", "translation_defect": true, "auditor": "labelaudit-batch-07"}
{"sid": "1196_100", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "Sort(text_list, StringLess) costs O(nlogn) per the table, and the following while loop compares at most n prefixes each bounded by the 100-character limit from the problem statement, so the label matches.", "auditor": "labelaudit-batch-07"}
{"sid": "1196_51", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The single while loop scans text_list once comparing each entry's fixed-length prefix against text, with no sort, so the Dafny is linear in n exactly as Python's `for a in l` loop, matching O(n).", "auditor": "labelaudit-batch-07"}
{"sid": "1235_283", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The single while loop over `matrix` does O(1) arithmetic per row (indices 0-3 only) and Join costs O(total output), so the Dafny is linear in n exactly as the Python's `for _ in range(n)` loop.", "auditor": "labelaudit-batch-07"}
{"sid": "1235_318", "label": "O(n*m)", "verdict": "mismatch", "true_class": "O(n)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes row width m grows with n. Check the problem statement: each test case always has exactly r,g,b,w (four balls), so `matrix[k]` is a fixed-width row of length >=3; if width never varies m is a constant, not a real dimension.", "evidence": "The inner `while j < |l|` loop scans `l`, but per the problem statement l is always the four values r,g,b,w, so its length is a fixed constant, not a growing m; the Python's `for i in l` loop is the same fixed-size scan, so both are O(n), not O(n*m).", "auditor": "labelaudit-batch-07"}
{"sid": "1243_0", "label": "O(nlogn+mlogm)", "verdict": "unsure", "true_class": "O(n+m)", "cause": "translation", "confidence": "low", "what_to_look_for": "The label assumes the Dafny mirrors Python's sorted()-based comparison at O(nlogn+mlogm). Check whether `multiset(s1) != multiset(s2)` in the Dafny/Python backend is implemented as a linear-time count map or as an incrementally-built structure like the O(n**2) set<T> backing; if it is linear the true class is O(n+m), if quadratic it is worse than the label.", "evidence": "The equality check `multiset(s1) != multiset(s2)` replaces Python's `sorted(g1)!=sorted(g2)`, and the cost of building and comparing a Dafny multiset is not in the given cost table, so whether the true class is O(n+m) or worse cannot be settled from the table alone.", "auditor": "labelaudit-batch-07"}
{"sid": "1243_178", "label": "O(n+m)", "verdict": "ok", "true_class": "O(n+m)", "cause": "", "confidence": "high", "evidence": "The single while loop scans s1 once (O(n)), and the two `a := a[i0 := ...]` seq updates after the loop each cost O(n) but occur only twice, not proportional to n, so the total stays O(n), matching the label's O(n+m) under the |s1|==|s2| requirement.", "auditor": "labelaudit-batch-07"}
{"sid": "1254_182", "label": "O(1)", "verdict": "ok", "true_class": "O(1)", "cause": "", "confidence": "high", "evidence": "The while loop iterates over the fixed 5-element literal array `[105,135,165,189,195]` and never touches n's magnitude in a loop bound, so the cost is O(1) regardless of n, matching the label and Python's `bisect_right` on the same fixed list.", "auditor": "labelaudit-batch-07"}
{"sid": "1254_187", "label": "O(n**2)", "verdict": "ok", "true_class": "O(n**2)", "cause": "", "confidence": "high", "evidence": "The nested `while i<=n` / `while j<=i` loops sum to Theta(n**2) divisor-counting operations, mirroring the Python's identical double `for` loop over `range(1,n+1,2)` and `range(1,i+1)`, so both are quadratic as labelled.", "auditor": "labelaudit-batch-07"}
{"sid": "1263_1960", "label": "O(nlogn)", "verdict": "mismatch", "true_class": "O(n)", "cause": "label", "confidence": "medium", "what_to_look_for": "The label assumes the per-test Sort call scales with n. Check that `res` never holds more than one entry per decimal digit of a_list[t]; since a_list[t] <= 10^4 per the problem statement, |res| <= 5 always, so Sort(res,...) is O(1) regardless of how many test cases n there are.", "evidence": "The inner `while v != 0` loop and the following `Sort(res, ...)` both operate on `res`, whose length is bounded by the number of decimal digits of a value <=10^4 (at most 5) per the problem statement, so each of the n test cases costs O(1) and the total is O(n), not O(nlogn); the Python shows the identical bound via `res.sort(reverse=True)` on the same digit list.", "auditor": "labelaudit-batch-07"}
{"sid": "1263_2538", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The inner `while i < ln` loop is bounded by ln = |IntToString(v)|, at most 5 digits since a_list[t] <= 10^4 per the problem statement, so each of the n outer iterations costs O(1), giving O(n) overall as labelled.", "auditor": "labelaudit-batch-07"}
{"sid": "1272_115", "label": "O(n**2)", "verdict": "mismatch", "true_class": "other", "cause": "translation", "confidence": "high", "what_to_look_for": "The label assumes the swap `list1 := list1[i := list1[j]][j := tmp]` is O(1) as Python's tuple-swap is. Check the innermost `if |list1[i]| > |list1[j]|` branch: it is a seq update on a length-n sequence, and for a reverse-length-sorted input it fires on nearly every one of the Theta(n**2) (i,j) pairs, giving Theta(n**3).", "evidence": "Inside the O(n**2) double loop over i and j, a triggered swap does `list1 := list1[i := list1[j]][j := tmp]`, a chained seq update costing O(n) per the table (real class O(n**3)), and worst-case reverse-length input triggers a swap on nearly every pair, versus Python's O(1) tuple swap `list1[i],list1[j]=list1[j],list1[i]`.", "translation_defect": true, "auditor": "labelaudit-batch-07"}
{"sid": "1272_278", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "Sort(strings, |x|<|y|) costs O(nlogn) per the table, the following while loop does one Contains1272b call per i bounded by the 100-character length cap from the problem statement, so the Dafny matches the Python's `arr.sort(key=len)` at O(nlogn).", "auditor": "labelaudit-batch-07"}
{"sid": "1276_39", "label": "O(n*m)", "verdict": "mismatch", "true_class": "other", "cause": "label", "confidence": "medium", "what_to_look_for": "The label O(n*m) assumes the loop count scales with N itself. Check `while (s+1)*(s+1) <= n`: it sets s to floor(sqrt(n)), and `l := 2*s+1` sizes every array that follows, so the inner DP loop over j<l runs O(sqrt(n)) times per k, not O(n) times; compare against the Python's identical `s=int(n**0.5)`.", "evidence": "`s` is computed as floor(sqrt(n)) via `while (s+1)*(s+1)<=n`, and `l:=2*s+1` bounds the size of `num0`, `num`, `prevRow` and `row`, so the DP loop `while i<k` nested with `while j<l` costs O(k*sqrt(n)), not O(n*m) (real class O(k*sqrt(n))); the Python's `s=int(n**0.5)` builds the identical sqrt(n)-sized `Num` array, so both share this sub-linear-in-n shape.", "auditor": "labelaudit-batch-07"}
{"sid": "1276_62", "label": "O(n+m)", "verdict": "mismatch", "true_class": "other", "cause": "label", "confidence": "medium", "what_to_look_for": "The label O(n+m) assumes the ws/dp arrays scale with n. Check the invariant `s == n - n/i` at the exit of the `while i<lim` loop: it implies extra = n - s = n/lim, and lim ~ sqrt(n), so m := partCount+extra is O(sqrt(n)), not O(n); the `while rep<k-1` loop then does O(m) work k-1 times.", "evidence": "`lim` is set via `while (r+1)*(r+1)<=n` so lim ~ sqrt(n), and `m := partCount+extra` with extra = n - s = n/lim per the loop invariant, making the ws/dp arrays size O(sqrt(n)); the `while rep<k-1` loop repeats the O(m) inner scan up to k-1 times, giving O(k*sqrt(n)) (real class) versus the label's O(n+m), and the Python's `lim = int((n+0.1)**0.5)+1` computes the identical bound.", "auditor": "labelaudit-batch-07"}
{"sid": "1303_56", "label": "O(1)", "verdict": "ok", "true_class": "O(1)", "cause": "", "confidence": "high", "evidence": "There are no loops or recursion; `p5` is a fixed 55-element literal and `k` is clamped by `length`, so every path costs O(1) regardless of n, matching the Python's constant-list slice `p5_list[:N]`.", "auditor": "labelaudit-batch-07"}
{"sid": "1303_8", "label": "O(n**2)", "verdict": "mismatch", "true_class": "O(1)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes the sieve's cost scales with n. Check the outer loop bound `i < 55556`: it is a hard-coded literal, not the parameter n, and n only appears in the early-exit `if |A|==n`; since n is constrained 5<=n<=55 by the problem statement and the loop's upper bound never depends on n's value, work done is bounded by a fixed constant.", "evidence": "The outer `while i < 55556` loop bound is the literal constant 55556, not the parameter n, and n only affects the early-exit test `if |A|==n`; the Python's identical `for i in range(3,55556,2)` shares this fixed cap, so the true cost never grows with n and is O(1), not O(n**2).", "auditor": "labelaudit-batch-07"}
{"sid": "1306_126", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "GroupCounts1306 makes at most 100 recursive calls since a_i is bounded to [1,100] by the problem statement (a genuine constant, not a dimension of k), each call costing O(k), giving O(k) total which the label expresses generically as O(n), matching the Python's O(m) Counter build.", "auditor": "labelaudit-batch-07"}
