{"sid": "2105_521", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The first loop builds b via b := b + [k] without reading b[i] in the same loop, O(1) amortized per append over n iterations; the second loop appends min(b[j],b[j+1]) similarly without reading ans[i], and MaxSeq(ans) is one O(|ans|) flatten afterward, giving O(n) total, matching the Python's single pass building b and ans.", "auditor": "labelaudit-batch-16"}
{"sid": "2124_138", "label": "O(n*m)", "verdict": "mismatch", "true_class": "O(n)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes query rows have a variable width m, but the problem statement fixes each query line to exactly three integers a, b, k. Open the description's 'three space-separated integers a, b, k' and confirm queries[i] is only ever indexed at 0, 1, 2; if so m is a constant and O(n*m) collapses to O(n).", "evidence": "The loop over |queries| reads only queries[i][0], queries[i][1], queries[i][2], a fixed-width row per the problem statement, so no dimension scales with row width; Join and IntToString are linear in output length, giving O(n) overall, same as the Python's two single passes over t queries.", "auditor": "labelaudit-batch-16"}
{"sid": "2124_2", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The while loop over |queries| reads only queries[i][0..2], a fixed three-integer row per the problem statement, and appends to answer without reading answer[i] in the loop; Join/IntToString are linear in output length, giving O(n), matching the Python's two single passes over t queries.", "auditor": "labelaudit-batch-16"}
{"sid": "2128_3", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "The first loop over n builds a and b via concatenation with no same-seq read, O(1) amortized per append; Sort on a and b is O(k log k) merge sort per the cost table, and the two scan loops afterward are O(|a|)+O(|b|), giving O(n log n) overall, matching the Python's two .sort() calls.", "auditor": "labelaudit-batch-16"}
{"sid": "2128_34", "label": "O(n**2)", "verdict": "unsure", "true_class": "O(n*m)", "cause": "label", "confidence": "low", "what_to_look_for": "The label O(n**2) assumes the inner loop's range scales with n (project count), but the inner loop actually runs from neg[i][0] to r, where r derives from m (initial rating) plus bounded per-item additions from pos, not from n. Check whether this dataset's convention treats a scalar signature parameter like m (used only to size an array, not as a second sequence length) as a growing dimension like other O(n*m) rows, or as a value the description caps ('1 <= r <= 30000') and hence constant; that choice moves true_class between O(n*m) and O(n).", "evidence": "Both the Python's 'for j in range(neg[i][0], r+1)' and the Dafny's 'while j <= r' scan a range sized by r, which starts at m and grows only by bounded per-item additions from pos capped at 300 each, so the double loop tracks n times an m-derived range rather than n squared, and the identical pattern in both sources means any error is in the label, not the translation.", "auditor": "labelaudit-batch-16"}
{"sid": "2166_288", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "SortInts(numbers) is O(k log k) merge sort per the cost table, followed by one O(|z|) scan counting values <= k2 and a single IntToString, giving O(n log n) overall, matching the Python's sorted(y) plus a linear scan.", "auditor": "labelaudit-batch-16"}
{"sid": "2166_84", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The Dafny is a single while loop over |numbers| with only O(1) arithmetic per iteration and no sort, giving O(n), matching the Python's single for loop over a with no sorted() call.", "auditor": "labelaudit-batch-16"}
{"sid": "2183_1", "label": "O(n*m)", "verdict": "mismatch", "true_class": "O(n)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes row width m grows, but the problem statement fixes each query to exactly 8 integers 'xp0 yp0 xp1 yp1 xp2 yp2 xp3 yp3'. Confirm the inner while loop's bound j < 8 is a literal constant, not tied to |row|; if so m is fixed and O(n*m) collapses to O(n).", "evidence": "The inner loop while j < 8 runs exactly 4 times regardless of n, building px/py of fixed length 4 via concatenation with no same-seq read, so per-query work is O(1) and the outer loop over n queries gives O(n), matching the Python's fixed-size p = [complex(...) for i in range(0,8,2)].", "auditor": "labelaudit-batch-16"}
{"sid": "2183_2", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The while loop over n rows reads a fixed 8-integer row (x0..y3) and does only O(1) arithmetic per iteration, appending to lines without reading lines[i], giving O(n), matching the Python's single for loop over q queries.", "auditor": "labelaudit-batch-16"}
{"sid": "2185_274", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The single while loop over |coord_list| (2n rows) does O(1) arithmetic per iteration accumulating a and b, no sort or nested scan, giving O(n), matching the Python's single for loop over 2n coordinate lines.", "auditor": "labelaudit-batch-16"}
{"sid": "2188_359", "label": "O(nlogn)", "verdict": "mismatch", "true_class": "O(n**2)", "cause": "translation", "confidence": "high", "translation_defect": true, "what_to_look_for": "The label assumes difmax updates are O(1) as in Python's list. Check whether difmax is a Dafny seq with a functional update `difmax := difmax[s := difmax[s] + 1]` inside the loop over n elements; if so, per the cost table each update is O(|difmax|)=O(n), making that loop alone O(n**2), dominating the two O(n log n) sorts.", "evidence": "The final loop over dif executes difmax := difmax[s := difmax[s] + 1], a seq functional update inside a loop of n iterations, costing O(n) per update and O(n**2) total for that loop, which dominates the two O(n log n) Sort calls; the Python's difmax[s]+=1 is an O(1) list index write, so the Python is O(n log n) as labelled.", "auditor": "labelaudit-batch-16"}
{"sid": "2188_371", "label": "O(n*m)", "verdict": "mismatch", "true_class": "O(n**2)", "cause": "both", "confidence": "high", "translation_defect": true, "what_to_look_for": "The label names a dimension m that the signature does not expose (a_list and b_list are both length n, and n is the only size parameter) -- confirm there is no second array or width in Solve's signature, which alone makes O(n*m) wrong. Separately, check whether ga, gb and counts are built via seq functional updates like `ga := ga[a_list[i] := i]` inside loops of n iterations; if so each update is O(n), making the Dafny O(n**2) even though the Python's dict writes ga[a[i]]=i are O(1).", "evidence": "Both while loops build ga/gb and counts via ga := ga[a_list[i] := i] and counts := counts[o := counts[o]+1], seq functional updates inside n-iteration loops, each costing O(n) and giving O(n**2) total for the Dafny, while the label's m dimension does not exist in the signature and the Python's dict writes ga[a[i]]=i, g[o]=... are O(1) amortized, giving O(n) for the Python.", "auditor": "labelaudit-batch-16"}
{"sid": "2193_138", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The while loop over |a_list| does O(1) comparisons per iteration counting occurrences of 1, 2, 3, and MaxSeq runs once over a fixed length-3 seq, giving O(n), matching the Python's single for loop over values.", "auditor": "labelaudit-batch-16"}
{"sid": "2193_70", "label": "O(nlogn)", "verdict": "mismatch", "true_class": "O(n)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes real sorting work scales with n, but the description states 'every number is from 1 to 3 inclusively', so distinct (built via `v !in distinct`) never exceeds length 3. Confirm the outer while loop over distinct is bounded by that constant rather than by n; if so the inner O(n) scan runs at most 3 times and Sort/SumSeq act on a length-<=3 seq, making the whole method O(n).", "evidence": "The outer while loop over distinct is capped at 3 iterations because a_list values are restricted to {1,2,3} per the description, so the inner O(n) scan over a_list runs at most 3 times giving O(n) total, and Sort/SumSeq then operate on a sequence of length at most 3, contributing O(1); the Python's dict-based freq counting over at most 3 keys is likewise O(n).", "auditor": "labelaudit-batch-16"}
{"sid": "2198_50", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The while loop is a rolling DP over i with O(1) arithmetic per iteration (d0, d1, maxi updated in place, no seq copy), giving O(n), matching the Python's single for loop building dp values.", "auditor": "labelaudit-batch-16"}
{"sid": "2198_52", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "Two O(n) build loops append to b and c without reading b[i]/c[i] in the same loop (O(1) amortized per append), followed by SortInts(c) at O(n log n) per the cost table, giving O(n log n) overall, matching the Python's two passes plus c.sort().", "auditor": "labelaudit-batch-16"}
{"sid": "2217_247", "label": "O(n**2)", "verdict": "ok", "true_class": "O(n**2)", "cause": "", "confidence": "high", "evidence": "The outer while loop over x (n queries) runs an inner while loop over |chains| (up to n) comparing chain[|chain|-1], giving O(n) scan work per outer iteration and O(n**2) total, matching the Python's `for y in a` inner scan per query.", "auditor": "labelaudit-batch-16"}
{"sid": "2217_419", "label": "O(n)", "verdict": "mismatch", "true_class": "O(n**2)", "cause": "translation", "confidence": "high", "translation_defect": true, "what_to_look_for": "The label assumes handle lookups are O(1) as in Python's dict. Open the Dafny and check whether 'old'/'new' lookups use a linear scan over a seq<string> keys (the fi/fj/fk while loops) rather than a hash-based map; if each of those three O(n) scans plus the slice-based delete keys[..popIdx]+keys[popIdx+1..] runs once per outer iteration, the whole method is O(n**2) regardless of the label.", "evidence": "Each of the n outer iterations over handles runs three linear scans over keys (the fi, fj, fk while loops, each O(|keys|)) plus, when a pop is needed, a slice concatenation keys[..popIdx]+keys[popIdx+1..] and a seq update vals[newIdx := val], all O(n); over n iterations this is O(n**2), while the Python's d[new]=d.get(old,old) and d.pop(old) are O(1) average dict operations, giving O(n).", "auditor": "labelaudit-batch-16"}
{"sid": "2225_120", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "SumSeq(a) is called once outside the loop (O(n)), and the while loop after it does only O(1) work per iteration comparing a[i] to the precomputed total minus a[i], giving O(n) total regardless of where the loop breaks.", "auditor": "labelaudit-batch-16"}
{"sid": "2225_154", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "SortInts(l) is O(n log n) per the cost table, followed by a single O(n) scan to find the index of the minimum value, giving O(n log n) overall, matching the Python's sorted(l) plus a linear scan.", "auditor": "labelaudit-batch-16"}
