{"sid": "2992_153", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "The loop builds `c` with `c := c + [(v, ceilVal, i)]` and no read of `c` inside the loop, so it is O(n); `Sort(c, ...)` dominates at O(n log n), matching the Python's `list.append` then `c.sort(...)`.", "auditor": "labelaudit-batch-23"}
{"sid": "2992_87", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "Single `while i < |a_list|` loop doing only O(1) arithmetic and comparisons per iteration (`ceilVal`, compare to `cc`), exactly mirroring the Python's single `for i in range(len(a))` scan.", "auditor": "labelaudit-batch-23"}
{"sid": "3017_340", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "Two sequential linear scans over `string_` (max-char search, then `count`) followed by `Repeat([t], count)` which is O(length of argument); all three stages are O(n), matching Python's scan plus `s.count` plus `str*count`.", "auditor": "labelaudit-batch-23"}
{"sid": "3017_45", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "`Sort(string_, ...)` is O(n log n) and dominates the trailing `while i < |sorted| && sorted[i] == top` loop, which only appends to `result` with no read of `result` inside the loop, so it is O(n); matches Python's `sort()`+`reverse()` then linear scan.", "auditor": "labelaudit-batch-23"}
{"sid": "3018_143", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "`SortInts(a_list)` costs O(n log n) and dominates the following linear `while i < |a|` divisibility check, matching Python's `a.sort()` followed by a plain `for x in a` scan.", "auditor": "labelaudit-batch-23"}
{"sid": "3018_50", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "`MinSeq(a_list)` is O(n) and the subsequent `while i < |a_list| && key` divisibility loop is also O(n), matching Python's `min(s)` call plus the `for i in s` scan with early break.", "auditor": "labelaudit-batch-23"}
{"sid": "3029_114", "label": "O(n+m)", "verdict": "mismatch", "true_class": "O(n**2)", "cause": "both", "confidence": "high", "what_to_look_for": "The label assumes near-linear cost, but both `while j < |heap|` and `while j < |heap2|` rebuild a size-N array by linear scan on every one of the N outer iterations (invariant `|heap|==N`), which is O(N) work times N outer steps. Open the inner while loops and confirm `|heap|` stays fixed at N through the outer loop, so total inner-loop iterations sum to N*N; separately, compare against the Python's `heapq.heappush`/`heapq.heappop`, which are true O(log N) heap operations, giving Python O(N log N) \u2014 so the label O(n+m) matches neither.", "evidence": "The Dafny simulates `heapq.heappush`/`heapq.heappop` with a from-scratch linear insertion scan (`while j < |heap|`) executed once per outer iteration over N iterations, giving O(N**2); the Python uses real `heapq` operations at O(log N) each, so Python is O(N log N), and O(n+m) matches neither source.", "translation_defect": true, "auditor": "labelaudit-batch-23"}
{"sid": "3029_74", "label": "O(nlogn)", "verdict": "mismatch", "true_class": "O(n**2)", "cause": "translation", "confidence": "high", "what_to_look_for": "The label O(nlogn) is right about the Python (`heapq.heappush`/`heapq.heappop` are O(log N) each, N iterations, O(N log N) total) but the Dafny replaces the heap with a linear-scan insertion (`while j < |R|` / `while j < |B|`) rebuilt on every one of N outer steps. Confirm the invariant `|R| == N` (resp. `|B| == N`) holds through the outer loop, so the inner scan is O(N) work repeated N times, giving O(N**2) \u2014 check this is the same shape as sibling `3029_114`.", "evidence": "Both `SUM1` and `SUM2` computation loops rebuild `newR`/`newB` via a full O(N) linear insertion scan on each of N outer iterations (`while j < |R|`, invariant `|R| == N`), giving O(N**2), while Python's real `heapq.heappush`/`heappop` are O(log N) each so the Python is O(N log N) as labelled.", "translation_defect": true, "auditor": "labelaudit-batch-23"}
{"sid": "3033_135", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "`Sort(pairs, ...)` costs O(n log n) and dominates the single trailing linear scan doing O(1) `AbsInt` work per iteration, matching Python's `a.sort(key=...)` followed by a plain `for i in range(n*2)` loop.", "auditor": "labelaudit-batch-23"}
{"sid": "3033_146", "label": "O(n)", "verdict": "mismatch", "true_class": "O(n**2)", "cause": "translation", "confidence": "high", "what_to_look_for": "The label assumes `pos0`/`pos1`/`seen` updates are O(1) as Python list writes are. Open the first `while i < |a|` loop and confirm the updates are seq functional updates `pos0 := pos0[v := i+1]` rather than array writes `a[i] := v`; if they are seq updates each costs O(n) (full copy per the cost table) and the loop runs O(n) times, giving O(n**2).", "evidence": "Inside the first `while i < |a|` loop (2n iterations), `pos0 := pos0[v := i+1]`, `pos1 := pos1[v := i+1]`, and `seen := seen[v := true]` are functional seq updates costing O(n) each, giving O(n**2) total, while Python's `b[a[i]].append(i+1)` on a list-of-lists is O(1) amortized per call so the Python is O(n) as labelled.", "translation_defect": true, "auditor": "labelaudit-batch-23"}
{"sid": "3034_1", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "The counting loop is O(n); the three append loops build `ans` with `ans := ans + [x]` and no read of `ans` inside those loops, so each is O(1) amortised per append; `JoinInts` is O(output length); all match Python's O(n) counting and list-building.", "auditor": "labelaudit-batch-23"}
{"sid": "3034_57", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "medium", "evidence": "`Sort(y, ...)` is O(n log n) and dominates; the two seq updates `sorted := sorted[i := sorted[1]]` and `sorted := sorted[1 := t]` execute once each after the search loop closes (not inside any loop), each costing O(n) once, which does not change the O(n log n) total, matching Python's `sorted()` plus two O(1) list index swaps.", "auditor": "labelaudit-batch-23"}
{"sid": "3046_103", "label": "O(n)", "verdict": "mismatch", "true_class": "O(n*m)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes only the floor count n matters, but the problem statement gives `1 <= n, m <= 100` as two independent parameters and each floor line has 2m characters that the inner loop `while i < 2*m` fully scans for every one of n floors. Confirm m is not fixed (unlike the 396_361 rectangle case) by rereading the input format, then check the inner loop bound `i < 2*m` runs to completion each outer iteration, giving O(n*m); compare directly against sibling `3046_65`, labelled O(n*m) for the identical problem.", "evidence": "The nested loops `while r < |v_3|` (n floors) and inner `while i < 2*m` (m flats per floor) together scan all n*m entries, and the Python does the same with `for _ in range(n)` plus `sum(... for i in range(0, m*2, 2))`, so both sources are O(n*m), not O(n) as labelled; sibling row 3046_65 for the same problem is correctly labelled O(n*m).", "auditor": "labelaudit-batch-23"}
{"sid": "3046_65", "label": "O(n*m)", "verdict": "ok", "true_class": "O(n*m)", "cause": "", "confidence": "high", "evidence": "Nested `while i < |v_3|` (n floors) and inner `while j < |arr|` (m flats) together scan all n*m window entries with O(1) work each, matching Python's `for i in range(n)` outer loop with an inner `while j<len(arr)` scan.", "auditor": "labelaudit-batch-23"}
{"sid": "3060_1359", "label": "O(n+m)", "verdict": "ok", "true_class": "O(n+m)", "cause": "", "confidence": "high", "evidence": "The `while true` loop does O(1) character work per iteration and runs at most min(|a|,|b|) times, which is bounded by n+m for the standard two-string-argument convention, matching Python's character-by-character comparison loop.", "auditor": "labelaudit-batch-23"}
{"sid": "3060_6262", "label": "O(nlogn)", "verdict": "mismatch", "true_class": "O(n+m)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes a real sort over an n-sized collection, but the Python's `sort(n[i], m[i])` helper calls `sorted(x+y)` where x and y are single characters, so it always sorts a length-2 string regardless of input size. Open the Python `sort` helper and confirm its argument is always 2 characters; then note the Dafny's equivalent is a plain `if n[i] > m[i]` comparison inside a single O(n+m) scan, with no log factor anywhere.", "evidence": "Python's `sort(n[i], m[i])` sorts a fixed 2-character string (`x+y` from two single-char indices) at O(1), not an n-sized collection, so the surrounding `for i in range(0, len(n))` loop is O(n) overall; the Dafny mirrors this with a direct `if n[i] > m[i]` comparison in a single scan, matching the O(n+m) convention used by sibling row 3060_1359 for the same problem, not O(nlogn).", "auditor": "labelaudit-batch-23"}
{"sid": "3070_180", "label": "O(nlogn)", "verdict": "ok", "true_class": "O(nlogn)", "cause": "", "confidence": "high", "evidence": "`SortInts(a_list)` costs O(n log n) and dominates the trailing O(n) summation loop, matching Python's `l.sort()` followed by a `for i in range(n-1)` sum.", "auditor": "labelaudit-batch-23"}
{"sid": "3070_20", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "`SumSeq(a_list)` and `MaxSeq(a_list)` are each O(n) with no loop in the method body itself, matching Python's `sum(a)`-equivalent loop plus `max(a)` call.", "auditor": "labelaudit-batch-23"}
{"sid": "3091_1574", "label": "O(n)", "verdict": "ok", "true_class": "O(n)", "cause": "", "confidence": "high", "evidence": "Single `while i < |pairs_list|` loop doing O(1) work per row (`pr[0]`, `pr[1]` reads and comparisons) since each row is a fixed two-element (p,q) pair per the problem statement, matching Python's `for i in range(n)` loop.", "auditor": "labelaudit-batch-23"}
{"sid": "3091_384", "label": "O(n*m)", "verdict": "mismatch", "true_class": "O(n)", "cause": "label", "confidence": "high", "what_to_look_for": "The label assumes a growing row-width m, but the problem statement fixes each room record to exactly two integers (pi, qi); check the input format confirms a fixed 2-element row like the 396_361 rectangle precedent, which makes m a constant, not a real dimension. Then confirm the first loop's `a := a + pairs_list[i]` never reads `a[...]` inside that loop (so it is O(1) amortised per row) and the second loop is a single O(n) scan over the flattened 2n-length array.", "evidence": "Each room record is a fixed two-element (pi, qi) pair per the problem statement, so `a := a + pairs_list[i]` concatenates a constant-size chunk n times with no read of `a` inside that loop, and the second `while idx+1 < |a|` loop is a single O(n) scan; Python's `a += list(map(int, ...))` plus its `while i<n*2` loop are likewise O(n), not O(n*m) as labelled \u2014 sibling row 3091_1574 for the same problem is correctly labelled O(n).", "auditor": "labelaudit-batch-23"}
